using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Gameplay : MonoBehaviour
{
    
    public int LevelNumber { get; private set; } = 0;   // Current level number
    public int LevelStatus { get; private set; } = 0;   // Current level finished or not (0: Opened, 1: Cleared)

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
